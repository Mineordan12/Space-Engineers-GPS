# Comments extracted from `se_gps_navigator.py`

This file preserves each source-code comment (except the required shebang) with its original line number. The comments were removed from the executable script.

- **Line 37:** `# ── Version ─────────────────────────────────────────────────────────`
- **Line 41:** `# Raw URL to a small JSON manifest in the GitHub repo, e.g.:`
- **Line 42:** `#   {"version": "2.3.0", "url": "https://raw.githubusercontent.com/<you>/<repo>/main/se_gps_navigator.py"}`
- **Line 43:** `# Leave UPDATE_MANIFEST_URL blank to disable update checking entirely.`
- **Line 109:** `# ── Config ──────────────────────────────────────────────────────────`
- **Line 110:** `#`
- **Line 111:** `# Connection settings are read from a small JSON config file and can`
- **Line 112:** `# be overridden with environment variables — handy if several people`
- **Line 113:** `# run this script from their own machines and don't want to hand-edit`
- **Line 114:** `# a file:`
- **Line 115:** `#`
- **Line 116:** `#   SE_GPS_DB_HOST      SE_GPS_DB_PORT     SE_GPS_DB_USER`
- **Line 117:** `#   SE_GPS_DB_PASSWORD  SE_GPS_DB_NAME`
- **Line 221:** `# ── DB resilience: retry-on-connect, retry-mid-operation ────────────`
- **Line 230:** `# attempts when first opening a connection`
- **Line 231:** `# extra attempts if a connection drops mid-operation`
- **Line 232:** `# seconds; multiplied by the attempt number`
- **Line 234:** `# MySQL error numbers that mean "this account isn't allowed to do`
- **Line 235:** `# that" — retrying won't help, so fail fast with a clear message`
- **Line 236:** `# instead of silently retrying a doomed request 3 times.`
- **Line 300:** `# +1 for the initial try`
- **Line 410:** `# No I, O to avoid confusion`
- **Line 427:** `# ── Data ────────────────────────────────────────────────────────────`
- **Line 502:** `# ── Stargate Name Generator ────────────────────────────────────────`
- **Line 504:** `# Clusters within this range of each other share a naming "prefix" so`
- **Line 505:** `# that a glance at two names hints whether the sites are close.`
- **Line 506:** `# 1000km`
- **Line 564:** `# Fallback with extended suffix`
- **Line 570:** `# ── GPS Parsing ─────────────────────────────────────────────────────`
- **Line 611:** `# ── Ore Detection ───────────────────────────────────────────────────`
- **Line 624:** `# word-boundary match so short aliases like "u" or "co" don't`
- **Line 625:** `# false-positive inside unrelated words`
- **Line 658:** `# ── Distance ────────────────────────────────────────────────────────`
- **Line 673:** `# ── Clustering ──────────────────────────────────────────────────────`
- **Line 675:** `# 100km`
- **Line 676:** `# 1.5km — same-ore markers this close get merged into one`
- **Line 725:** `# Word-boundary-ish match that still works around hyphens/digits`
- **Line 849:** `# New cluster. Cluster names are UNIQUE in the DB, so if two players`
- **Line 850:** `# add a brand-new site at the same moment and happen to roll the same`
- **Line 851:** `# Stargate name, retry with a fresh name instead of failing.`
- **Line 969:** `# ── UI Helpers ──────────────────────────────────────────────────────`
- **Line 1007:** `# ── Add GPS ─────────────────────────────────────────────────────────`
- **Line 1191:** `# Detect ore type(s) — a single deposit can hold more than one`
- **Line 1192:** `# resource. Stations don't have a resource, so skip this entirely.`
- **Line 1204:** `# resolved lazily, only if a new entry is actually needed`
- **Line 1258:** `# Declined the split — fall through as a single entry using one ore`
- **Line 1270:** `# If a same-ore marker already exists nearby, merge into it instead of`
- **Line 1271:** `# creating a near-duplicate (e.g. two people reporting the same vein).`
- **Line 1282:** `# Resolve the cluster this point belongs to`
- **Line 1285:** `# Work out a suggested name for the entry itself`
- **Line 1287:** `# The name already carries the cluster reference — use it as-is`
- **Line 1290:** `# Cluster name goes in front of the ore, e.g. "X3C-395 FE"`
- **Line 1317:** `# ── Search GPS ──────────────────────────────────────────────────────`
- **Line 1349:** `# Determine if ore search or name search`
- **Line 1361:** `# Fall back to a word-boundary text match (e.g. an`
- **Line 1362:** `# un-split multi-resource entry that still mentions this`
- **Line 1363:** `# ore in its name/description) — never a bare substring,`
- **Line 1364:** `# so "u" (uranium) can't match inside "au" (gold).`
- **Line 1371:** `# Name/cluster search`
- **Line 1415:** `# ── List All ────────────────────────────────────────────────────────`
- **Line 1425:** `# "ore" or "cluster"`
- **Line 1475:** `# ── Rename ──────────────────────────────────────────────────────────`
- **Line 1763:** `# ── Where Am I ──────────────────────────────────────────────────────`
- **Line 1837:** `# ── Main Menu ───────────────────────────────────────────────────────`
- **Line 1908:** `# ── Entry Point ─────────────────────────────────────────────────────`
